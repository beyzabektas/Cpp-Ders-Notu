#include <iostream>

int main()
{
    int a = 0;
    int b = 0;
    char islem = '+';
    
   /* while(true)
    {
    std::cout  << "Birinci sayi" << std::endl;
    std::cin >> a;
    std::cout  << "İkinci sayi" << std::endl;
    std::cin >> b;
    
    std::cout << "İslem giriniz" << std::endl;
    std::cin >> islem;

    if (islem == 'e')
    {
        go to spot;
    }

    /* //if (islem == '+')
    if(a <= 10)
    {
        std::cout << "Sonuc:" << a + b;
    }
    else
    {
        std::cout << "İslem yok";
    }
    
     switch(islem)
     {
        case '+':
            std::cout << "Sonuc:" << a + b;
            break;
        case '-':
            std::cout << "Sonuc:" << a - b;
            break;
        case '*':
            std::cout << "Sonuc:" << a * b;
            break;
        case '/':
            std::cout << "Sonuc:" << a / b;
            break;
        default:
            std::cout << "Bilinmeyen islem"; 
            break;
     }
    }
    std::cout << "Devam";*/
/* for (size_t i = 0; i < 10; i++)
{
        std::cout << "Dongu" << i << std::endl;
} */

   /* do
    {
        std::cout << "Bilinmeyen islem"; 
    }
    while(false);
    */

   //Go to
    stop:
         std::cout << "Durdu"; 

} 

  
   
